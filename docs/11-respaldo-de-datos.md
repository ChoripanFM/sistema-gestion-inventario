# Migración o respaldo de datos

Si se quiere conservar los datos, es necesario copiar y almacenar estos dos elementos:

- inventario.db → base de datos con todos los productos y categorías
- server/uploads/ → carpeta con todas las imágenes subidas

Sin estos archivos el sistema arranca limpio.
